name = "story-server"
main = "worker.js"
compatibility_date = "2024-01-01"

# After running: npx wrangler kv:namespace create STORIES
# Replace the ID below with the one you receive
kv_namespaces = [
  { binding = "STORIES", id = "YOUR_KV_NAMESPACE_ID" }
]
